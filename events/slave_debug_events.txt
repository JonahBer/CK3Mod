namespace = slave_debug
slave_debug.1 = {
	type = character_event
	# CK3 events do not use is_triggered_only (events are already script-triggered).
	# Keeping a theme avoids "Theme missing" errors in the log.
	theme = default
	title = debug_slave_flags_title
	desc = debug_slave_flags_tt
	option = {
		name = OK
	}
}
